const nameInput = document.getElementById('my-name-input');
const messageInput = document.getElementById('message');
const sendButton = document.getElementById('send-button');
const chatBox = document.getElementById('chat');

sendButton.addEventListener('click', () => {
  
    //const id = messageInput.dataset.id;
    const sender = nameInput.value;
    const text = messageInput.value;
    const timestamp = new Date(message.time);
    const time = message.getHours() + ":" + message.getMinutes() + ":" + message.getSeconds();  
    
    const message = {id, text, sender, timestamp};
});

function fetchMessage(){

};

function updateMessageInChatBox(){

}

sendMessages.addEventListener(click,sendButton){
  

}